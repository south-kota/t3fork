{
  "name": "@t3tools/marketing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:schemas": "bun ../../scripts/build-server-settings-schema.ts",
    "dev": "bun run build:schemas && astro dev",
    "build": "bun run build:schemas && astro build",
    "preview": "astro preview",
    "typecheck": "astro check"
  },
  "dependencies": {
    "astro": "^6.0.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.7",
    "typescript": "catalog:"
  }
}
